{
  "name": "war3-maps-site",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-site.mjs",
    "dev": "astro dev",
    "build": "npm run generate && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@wesleyel/war3parser": "^0.4.0",
    "astro": "^5.0.0"
  }
}
